# Projects: Small Apps

This folder contains small project scripts moved from the repository root:

- `expense_tracker.py` — simple interactive expense tracker
- `notes_app.py` — simple file-backed notes application

Run any script with:

```bash
python 09-projects/project/expense_tracker.py
python 09-projects/project/notes_app.py
```
